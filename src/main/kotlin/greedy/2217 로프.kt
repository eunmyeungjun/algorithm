package 그리디
/*
import java.util.Scanner

fun main(){
    val sc:Scanner =Scanner(System.`in`)
    val ropes =sc.nextInt()

    val ropes = readLine()?.split(" ")?.mapNotNull { it.toInt() } ?: emptyList()

   if(ropes.isNotEmpty() ){
        val sortedRopes = ropes.sorted()
        var heavyWeight = 0

        for( i in sortedRopes.indices){
            val weight = sortedRopes[i] * (sortedRopes.size - i )
            if (weight >= heavyWeight) {
                heavyWeight = weight}

        }
        println("$heavyWeight")
   }
}
*/
