package 그리디
/*

fun main()=with(System.`in`.bufferedReader()) {
    val paperCount = readln().toInt()
    val whitePaper = Array(100, { IntArray(100, { 0 }) })
    val (x, y) = readln().split(" ").map { it.toInt() }

    for (i in x until x + 11){
        for(j in y until y+11){
            whitePaper[i][j]=1
            }
        }
    println(whitePaper.toString().count(it=='1'))
    }



이어서하기
9.29 whitePaper[i][j]가 빨간색 에러뜨는 이유 알아보기(2차원 배열 선언 방법이 잘못됫는지 찾아보기) ㅇㅇ
2차원배열 ->.count()쓸수 있는 방법 찾기 toString 안댐>?
*/




